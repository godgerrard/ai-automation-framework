"""
MCP Server package for AI Automation Framework.

Note: directory is named mcp_server (not mcp) to avoid shadowing the
installed `mcp` / `fastmcp` packages when the repo root is on sys.path.

Start the server:
    python mcp_server/server.py
or via fastmcp:
    fastmcp run mcp_server/server.py
"""
